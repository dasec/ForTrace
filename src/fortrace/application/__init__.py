"""The application package contains the implementation of supported applications.

Currently, webBrower (Firefox, Chrome, Safari), mailClient (Thunderbird), instantMessaging (Pidgin) are supported.
Every class in webBrowser, mailClient... is derived from module application which contains the base classes.
If you want to add a new application, copy 'skeleton.py' and edit it.

"""
