"""This package contains an api for mouse clicks and keyboard strokes.

The agent will call inputDevice.py which will parse the arguments from the agent. After the next function
(mouse or keyboard) will be called.

"""
